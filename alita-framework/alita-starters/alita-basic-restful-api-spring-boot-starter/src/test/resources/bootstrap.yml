spring:
  application:
    name: basic-restful-api-test
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        namespace: alita-dev
        group: ALITA_TEST_GROUP
        serverAddr: 127.0.0.1:8848
        fileExtension: yml
      discovery:
        namespace: yd-alita-web-service
        group: YD_ALITA_BASE_GROUP